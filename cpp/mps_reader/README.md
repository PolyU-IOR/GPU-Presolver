# Bundled MPS Reader

This directory contains the continuous-MPS reader used by the
GPU-Presolver command-line tools and parser regression tests. It is built as
the `gpu_presolver_mps_reader` CMake target and requires no external download.

The reader is part of the GPU-Presolver source distribution and is covered
by the repository's top-level MIT license.
